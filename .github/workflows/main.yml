name: Wake up Apps

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send requests
        run: |
          curl -I https://solaridehorizon.streamlit.app/
          curl -I https://creuse-cine-plus.streamlit.app/
